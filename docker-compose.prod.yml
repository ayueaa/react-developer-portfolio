version: '3.8'
services:
  app:
    build:
      context: .
      image: react-developer-portfolio_app:prod
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
