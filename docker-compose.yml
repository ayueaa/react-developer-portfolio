version: '3.8'
services:
  app:
    build: .
    image: react-developer-portfolio_app:dev
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
